import * as Path from 'path';

export const BASEMAN_DIR = Path.resolve('test/baseman');
export const BASELINE_PATH = Path.join(BASEMAN_DIR, 'baseline');
export const REFERENCE_PATH = Path.join(BASEMAN_DIR, 'reference');
