import * as Path from 'path';

export const BASEMAN_DIR = Path.resolve('test/baseman');
export const BASELINE_DIR = Path.join(BASEMAN_DIR, 'baseline');
export const REFERENCE_DIR = Path.join(BASEMAN_DIR, 'reference');
